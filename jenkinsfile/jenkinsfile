pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Baut Java-Application mit Maven
                sh '''
                cd /var/lib/jenkins/workspace/Scripted-Build/customerapi
                pwd
                mvn install
                pwd
                '''
            }
        }
    }
}
