pipeline {
    agent any
    stages {
        stage('Pre-Build') {
            steps {
                sh '''
                cd customerapi
                mvn install
                '''
            }
        }
    }
}
