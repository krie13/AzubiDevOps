pipeline {
    agent any
    stages {
        stage('Pre-Build') {
            steps {
                sh '''
                mvn install
                '''
            }
        }
    }
}
