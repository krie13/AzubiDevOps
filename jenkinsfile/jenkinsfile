pipeline {
    agent any
    stages {
        stage('Pre-Build') {
            steps {
                sh '''
                cd /data/AzubiDevOps/customerapi
                echo adminadmin | sudo -S mvn install
                '''
            }
        }
    }
}
