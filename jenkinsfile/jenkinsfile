pipeline {
    agent any
    stages {
        stage('Pre-Build') {
            steps {
                sh '''
                cd /data/AzubiDevOps/customerapi
                sudo mvn install
                '''
            }
        }
    }
}
